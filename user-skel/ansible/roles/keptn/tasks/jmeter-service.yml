- name: Keptn - Install JMeter service
  become_user: "{{ non_root_user }}"
  shell: helm upgrade --install jmeter-service -n {{ keptn_namespace }} https://github.com/keptn/keptn/releases/download/{{ keptn_version }}/jmeter-service-{{ keptn_version }}.tgz
