config: 
    - acebox: "kubernetes.json"
  
acebox:
    - name: "ACE-BOX"
    # endpointUrl and authToken are sourced from environment in json file.
    # This is a workaround until Monaco offers the option to skip 
    # environment variable validation when a project is not deployed.
    # - endpointUrl: ""
    # - authToken: ""
