---
- hosts: localhost
  connection: local
  become: true
  vars:
    ace_config_file_path: "./ace.config.yml"
  tasks:
  - name: Prepare config
    include_tasks:
      file: roles/config/tasks/init.yml
